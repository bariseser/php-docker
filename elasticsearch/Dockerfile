ARG ELASTICSEARCH_VERSION
ARG ELASTICSEARCH_HOST_HTTP_PORT
ARG ELASTICSEARCH_HOST_TRANSPORT_PORT

FROM elasticsearch:${ELASTICSEARCH_VERSION}

EXPOSE ${ELASTICSEARCH_HOST_HTTP_PORT} ${ELASTICSEARCH_HOST_TRANSPORT_PORT}
